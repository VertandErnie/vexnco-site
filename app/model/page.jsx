import PageSection from "../components/PageSection";

export default function ModelPage() {
  return (
    <PageSection
      eyebrow="2 · VE Model"
      title="VE as the operational physics model."
      description="VE formalizes the organization itself: entities, relationships, behaviors, and constraints. It is the model NCO runs on top of and the foundation for drift detection and autonomy."
    />
  );
}
